# CR3 Sim2Real 控制系统调试经验与诊断规范

更新时间：2026-08-18

## 1. 文档目的

本文档记录本项目在 MuJoCo、Quest、IK、TCP/IP 和 CR3 实机控制上的调试方法与已验证经验。

文档中的结论分为三类：

- **已证实**：可以由代码、日志或可重复实验直接确认。
- **高概率**：现象与机制一致，但仍需单变量实验确认。
- **待验证**：目前只能作为实验假设，不能当作结论。

禁止把“某个参数改变后现象变了”直接等同于“该参数就是根因”。必须保留基线、逐项改变，并记录输入、输出和时间信息。

## 2. 当前可复现基线

`2fc5b230b39343a7fa1b597036a588a71ee9a5d2` 是目前已知的实时同步平滑基线。

当前代码通过以下回退提交恢复到该基线：

```text
9f2a986 restore control code to 2fc5b230 baseline
```

当前验证结果：

```text
53 tests passed
```

后续改动没有从 Git 历史中删除，可以通过提交历史恢复或逐项比较。

## 3. 控制链路的准确划分

### 3.1 通用实时同步链路

键盘、GUI点动以及部分手部模式最终都会进入：

```text
输入设备
  -> q_target（MuJoCo关节目标，弧度）
  -> sim_rad_to_real_deg
  -> LiveServoHardware.set_stream_target
  -> 固定周期ServoJ worker
  -> DobotApiMove.ServoJ
  -> CR3控制器
```

`q_target`是主机计算出的目标，不是实体机械臂的实际反馈。

### 3.2 Quest链路

```text
Quest UDP wrist XYZ
  -> Unity坐标系到CR3坐标系
  -> 减去Quest原点
  -> Quest位置滤波/预测
  -> Link6位置误差
  -> MuJoCo阻尼伪逆IK
  -> q_target
  -> 实机发送链路（仅启用实机时）
```

Quest的Quaternion目前不参与末端旋转控制。Quest模式进入接管前会回到独立的水平工具Home，
固定目标姿态为`RX=+92.43°、RY=+0.59°、RZ=+88.17°`，使TCP Z轴近似平行世界`+X`；Quest只改变XYZ。
原有的通用Home仍用于键盘、回放和普通实时同步。

### 3.3 反馈链路

30004反馈线程独立读取：

- 实际关节角；
- 实际关节速度；
- 机器人模式、使能和报警状态；
- TCP位姿和力/力矩字段（如果控制器提供）。

当前实时ServoJ控制主要使用反馈做安全检查和失联检测，并不构成闭环的位置误差控制器。除非代码明确使用`joints_deg`修正下一条命令，否则不能把它称为“反馈闭环跟踪”。

## 4. 必须区分的四种量

调试日志必须明确区分以下变量：

| 名称 | 含义 |
|---|---|
| requested target | 上层传给硬件层的目标关节角 |
| filtered target | 硬件层滤波后的目标 |
| planned target | 经过速度/加速度限制后真正发送的目标 |
| actual feedback | 30004读到的实体关节实际角度 |

只看`Send ServoJ`日志，无法判断振动来自上游目标、硬件层限幅还是实体执行误差。至少需要同时记录`requested target`、`planned target`和发送时间戳。

## 5. 已确认的回归差异

对比平滑基线`2fc5b230`与后续失败版本，发现以下直接影响实时同步的改动：

| 项目 | 平滑基线 | 后续版本 | 影响判断 |
|---|---|---|---|
| ServoJ `t` | `0.10 s` | 先改为`0.05 s`，再改为`0.03 s` | **高概率影响**控制器内部轨迹时间 |
| lookahead | `50` | 改为`100` | **待单变量验证**，不能单独归因 |
| 实机目标EMA | `slow=0.55, fast=0.90` | 后来改为`1.0, 1.0` | **高概率影响**离散目标的平滑程度 |
| 限速时间步长 | 固定`0.03 s` | 改用实际发送间隔，最大`0.05 s` | **高概率影响**命令步长一致性 |
| 换向处理 | 无额外状态 | 增加换向减速度 | **不是根因证明**，属于后续补丁 |
| Quest预测/速度状态 | 原始位置滤波 | 增加预测、速度估计和新时间语义 | 只影响Quest模式 |
| Quest IK | 普通位置增量IK | 姿态保持IK并累积`q_target` | 只影响Quest模式 |
| MuJoCo显示速度 | `60 deg/s` | `180 deg/s` | 只影响仿真显示 |

因此，普通实时同步出现回归时，应先检查`hardware.py`，不能先归因于Quest或IK。

## 6. 日志判读方法

### 6.1 目标步长是否达到限速上限

例如发送周期为30ms、主机速度限制为18°/s：

```text
最大单步 = 18 × 0.03 = 0.54°
```

如果日志连续出现约`±0.54°`的关节步长，说明主机限速器正在饱和。此时需要进一步确认目标是否变号，不能直接认为网络丢包。

### 6.2 目标变号

类似下面的序列：

```text
8.1478 -> 8.6800 -> 9.2130 -> 8.6783 -> 8.1446
```

表示命令先正向、随后反向。可能来源包括：

1. 上层`q_target`本身反向；
2. 硬件层滤波或限速状态产生反向；
3. 控制器对相邻ServoJ点的执行方式与主机假设不一致。

仅凭该序列不能判断是哪一层，必须同时记录`requested target`和`planned target`。

### 6.3 发送间隔

应记录：

```text
send_dt = 当前ServoJ发送时间 - 上一次ServoJ发送时间
servo_rtt = 回复时间 - 发送时间
```

判断规则：

- `send_dt`稳定、`planned target`平滑、实机仍振动：优先检查ServoJ参数或控制器执行；
- `send_dt`不稳定：优先检查线程调度、TCP调用耗时和发送周期设计；
- `planned target`不稳定但`requested target`平滑：优先检查硬件层滤波/限速；
- `requested target`已经不稳定：优先检查IK、Quest映射或输入数据。

### 6.4 实际反馈误差

应同时计算：

```text
tracking_error = actual_feedback - planned_target
```

如果误差持续增大，说明实体执行跟不上；如果误差很小但机械臂仍震动，问题更可能在命令轨迹或控制器参数，而不是机械臂未到位。

## 7. 单变量回归实验规范

每次实验必须满足：

1. 从同一个 Git 基线开始；
2. 只改变一个控制变量；
3. 记录机器人IP、使能状态、速度限制和控制模式；
4. 保存至少1秒的命令与反馈日志；
5. 先在低速、空旷工作区进行；
6. 出现异常立即停止，不用继续“试到它自己恢复”。

推荐顺序：

1. `2fc5b230`原样运行，确认平滑基线；
2. 只改变ServoJ `t`；
3. 恢复后只改变`lookahead_time`；
4. 恢复后只改变硬件目标EMA；
5. 恢复后只改变限速时间步长；
6. 最后单独测试Quest滤波和IK。

每一步都要保留结果，不允许同时改变多个因素后再凭体感判断。

## 8. 运动学调试原则

### 8.1 末端定义

当前MuJoCo末端使用`Link6` body。它不自动等价于Studio中的自定义TCP site。

如果仿真与实体的末端位置存在固定偏移，应单独检查：

- Link6 body原点；
- TCP site位置；
- 工具坐标系旋转；
- Studio当前工具坐标配置。

不能用调滤波的方法修正坐标系偏移。

### 8.2 IK与实时发送是两个问题

IK负责生成`q_target`，ServoJ层负责将关节目标发送给实体。一个模式在MuJoCo中正确，不代表ServoJ发送轨迹一定平滑；反过来也一样。

因此必须分别测试：

1. 只运行MuJoCo，不发送实体；
2. 发送预先生成的平滑关节轨迹；
3. 再接入Quest或键盘实时目标。

## 9. 安全规则

- 实机测试必须在低速和可触发急停的条件下进行；
- 不绕过使能、报警和反馈检查；
- 不为了验证平滑性而删除速度限制；
- 不把`EmergencyStop`当作正常停止方式；
- 不在目标、反馈和发送时间未记录的情况下判断控制器参数；
- 任何回退或参数实验都必须保留Git提交。

## 10. 当前下一步

当前源代码已恢复到已知平滑基线。下一步应先补充发送诊断字段，而不是继续修改Quest或IK：

```text
timestamp
send_dt
servo_rtt
requested_target
filtered_target
planned_target
actual_feedback
tracking_error
```

拿到同一段动作的完整数据后，才能准确判断是：

```text
上层目标问题
硬件层轨迹问题
TCP发送节拍问题
还是CR3控制器ServoJ执行问题
```
