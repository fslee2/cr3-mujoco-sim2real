# CR3 实时同步顿挫调试记录

更新时间：2026-08-18

## 当前结论

已确认 `2fc5b230b39343a7fa1b597036a588a71ee9a5d2` 版本的实时同步控制明显更丝滑。当前代码已经通过提交 `9f2a986` 恢复到该版本的源代码状态，后续改动没有被直接删除，仍保留在 Git 历史中。

因此下一步不能继续凭感觉叠加滤波或调速，而应该以 `2fc5b230` 作为基准，每次只改变一个因素。

## 重要区分：通用实时同步 vs Quest实时同步

### 通用实时同步

键盘、GUI点动和部分手部模式最终都经过：

```text
q_target
  -> sim_rad_to_real_deg
  -> LiveServoHardware.set_stream_target
  -> 33 Hz ServoJ worker
  -> ServoJ
```

这一条链路的核心代码在：

`cr3_sim2real/hardware.py`

### Quest实时同步

Quest还会在进入上述硬件链路之前经过：

```text
Quest wrist XYZ
  -> 坐标变换和原点
  -> Quest滤波/预测
  -> IK
  -> q_target
  -> LiveServoHardware
```

因此，`run_keyboard_sim2real.py`和`quest_hand.py`的改动只会影响Quest/IK模式；它们不会解释普通键盘实时同步为什么变顿挫。普通实时同步首先应只检查`hardware.py`。

## 2fc5b230与后续版本的关键差异

| 项目 | 2fc5b230基线 | 后续失败版本 | 可能影响 |
|---|---|---|---|
| ServoJ `t` | `0.10 s` | `0.05 s`，最后改为`0.03 s` | 控制器内部每个目标点的运行时间改变 |
| `lookahead_time` | `50` | `100` | 改变控制器内部的前馈/阻尼行为 |
| 实机目标EMA | slow `0.55`，fast `0.90` | 后来改成`1.0/1.0` | 取消实机端平滑，目标跳变直接进入ServoJ |
| 限速时间步长 | 固定`0.03 s` | 改为实际TCP间隔，最长`0.05 s` | 网络/调度抖动会变成不同的关节步长 |
| 换向处理 | 无额外状态 | 增加换向减速度状态 | 改变反向时的目标序列，但没有解决根因 |
| Quest预测 | 无预测 | 增加速度估计和预测，再后来关闭 | 只影响Quest，不影响普通实时同步 |
| Quest IK | 普通6维IK、从当前姿态求解 | 姿态保持IK、累积`q_target` | 只影响Quest，不影响普通实时同步 |
| MuJoCo显示速度 | `60 deg/s` | `180 deg/s` | 只影响仿真显示，不直接控制实机 |

## 用户发送日志的证据

日志中的J1/J6序列为：

```text
J1: 8.1478 -> 8.6800 -> 9.2130 -> 8.6783 -> 8.1446
J6: 5.4575 -> 5.9412 -> 6.4258 -> 5.9397 -> 5.4546
```

相邻目标的变化约为`±0.53 deg`，而当前限速正好是：

```text
18 deg/s * 0.03 s = 0.54 deg
```

这说明当时发送器已经在速度上限运行，并且目标出现了快速换向。它不是简单的TCP丢包，因为每条命令都得到了`0,{}`成功回复；问题更像是：

1. 上层目标本身在变号；或
2. 实机端滤波被取消后，目标变号直接传给了ServoJ；或
3. `t`、发送周期和控制器内部规划时间不匹配。

## 当前最可信的原因排序

### 1. 后续版本取消了2fc中的实机端目标平滑

2fc使用：

```python
LIVE_TARGET_FILTER_SLOW_ALPHA = 0.55
LIVE_TARGET_FILTER_FAST_ALPHA = 0.90
```

这会把离散的`q_target`变化先缓冲一层。后续改成`1.0/1.0`后，任何上层目标反向都会立即成为ServoJ目标反向。

这与用户观察到的“2fc丝滑、后续版本震动”高度吻合，应该优先恢复验证。

### 2. `t`从0.10改成0.03

2fc每个ServoJ目标点持续时间是100ms，后续版本变成30ms。即使主机仍然每30ms发送一次，控制器看到的内部轨迹也已经完全不同。

不能假设`t`必须等于主机发送周期；这需要以CR3控制器实际行为做A/B测试。

### 3. 固定时间步长改成实际TCP间隔

基线一直使用固定`0.03 s`来计算关节步长，因此同样的目标变化产生稳定的命令幅度。后续使用实际间隔后，Windows调度或TCP耗时会直接改变步长，可能形成可见的速度波动。

### 4. 换向减速度补丁不是根因修复

后续加入的`LIVE_DIRECTION_CHANGE_ACCEL_DEG_S2`只是在ServoJ输出端拦截换向。它无法解决上游`q_target`为什么反向，也可能让控制产生额外滞后。因此它已经随回退被移除。

## 建议的单变量实验顺序

以当前已恢复的2fc代码为基线，逐项做实验：

1. **基线确认**：不修改代码，确认普通实时同步仍然丝滑。
2. 只把`LIVE_SERVO_T_S`从`0.10`改为`0.03`，其他保持2fc。
3. 只把`lookahead_time`从`50`改为`100`。
4. 只把目标EMA改成`1.0/1.0`。
5. 只把固定`control_dt=0.03`改成实际发送间隔。
6. 最后才检查Quest的IK和目标轨迹。

每次只改一项，并记录：

```text
发送时间戳
ServoJ请求目标
滤波后目标
限速后目标
ServoJ调用耗时
30004实际关节角
实际关节速度
```

## 判断标准

### 如果请求目标已经反向

问题在Quest/键盘到`q_target`的上游，需要检查IK目标生成。

### 如果请求目标平滑，但限速目标反向

问题在`LiveServoHardware`的滤波、限速或时间步长。

### 如果发送目标平滑，实体机仍震动

问题在ServoJ参数组合或控制器内部执行，需要固定发送周期并逐个测试`t`和`lookahead_time`。

### 如果发送间隔不稳定

问题在Python线程/TCP发送节拍，需要继续做固定周期发送和发送耗时统计。

## 当前代码状态

当前源文件与`2fc5b230`一致：

```text
9f2a986 restore control code to 2fc5b230 baseline
```

验证结果：

```text
53 tests passed
```

生成的`__pycache__`变化不属于控制源代码，可以忽略。
